define(['source'], function(MyModule) {

  describe('MyModule', function () {
    describe('nothing', function () {
      it('should do nothing', function () {
        // do nothing, for now.
      });
    });
  });
  
});